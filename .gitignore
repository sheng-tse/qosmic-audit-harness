node_modules/
*.zip
.DS_Store
**/.DS_Store
takehome/
__MACOSX

# Crawl captures are regenerated by scripts/crawl.mjs; the screenshots and raw
# data are heavy and store-specific, so keep them out of git.
sample_output/**/crawl/

# This repo's .claude/ IS the deliverable, so override the global ignore of it.
# Keep Claude Code's local-only state out, though.
!.claude/
!.claude/**
.claude/settings.local.json
